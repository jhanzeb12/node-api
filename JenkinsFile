pipeline {
    agent any
    tools{
        nodejs "node"
    }
    stages{
        stage('Checkout') {
            steps {
                bat 'git clone https://github.com/jhanzeb12/node-api.git'
            }
        }

        stage('Build') {
            steps {
                bat 'npm i'
            }
        }
    }
}